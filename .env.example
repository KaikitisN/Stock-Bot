# ── Alpaca (paper trading first) ──────────────────────────────────────────
ALPACA_API_KEY=your_alpaca_key
ALPACA_SECRET_KEY=your_alpaca_secret
ALPACA_PAPER=true

# ── AI provider keys (fill in only the ones you plan to use) ──────────────
PERPLEXITY_API_KEY=your_perplexity_key
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key

# ── Kronos local model (no API key needed) ────────────────────────────────
# Path to your cloned Kronos repo (relative or absolute)
KRONOS_REPO_PATH=../Kronos
# Model size: mini (~1 GB, CPU ok) | small (~2 GB) | base (~4 GB) | large (~8 GB GPU)
KRONOS_MODEL_SIZE=small
